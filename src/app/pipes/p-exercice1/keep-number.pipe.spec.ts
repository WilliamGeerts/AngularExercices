import { KeepNumberPipe } from './keep-number.pipe';

describe('KeepNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new KeepNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
